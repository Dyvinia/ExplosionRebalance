{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}+${minecraft_version}",
  "name": "${mod_name}",
  "description": "${description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "homepage": "https://github.com/Dyvinia/ExplosionRebalance",
    "sources": "https://github.com/Dyvinia/ExplosionRebalance",
    "issues": "https://github.com/Dyvinia/ExplosionRebalance/issues"
  },
  "license": "${license}",
  "icon": "${mod_id}.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "org.dyvinia.explosionrebalance.ExplosionRebalance"
    ]
  },
  "mixins": [
    "${mod_id}.mixins.json",
    "${mod_id}.fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": "*",
    "minecraft": "${minecraft_version_fabric_range}",
    "java": ">=${java_version}"
  },
  "suggests": {
    "another-mod": "*"
  }
}
